[project]
name = "model-understanding-rl-v2"
version = "0.1.0"
description = "Prime RL Verifiers environment for model-understanding — batch comparative judging with Opus"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "datasets",
    "verifiers",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
